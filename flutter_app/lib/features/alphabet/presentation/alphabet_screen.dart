import 'package:flutter/material.dart';

/// Placeholder — pełna implementacja w Kroku 9.
class AlphabetScreen extends StatelessWidget {
  const AlphabetScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Alfabet')),
      body: const Center(child: Text('Alphabet — krok 9.')),
    );
  }
}
